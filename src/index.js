// import React from 'react';
import ReactDOM from 'react-dom';

/**
 * JSX只是react提供的一个语法糖
 * 老的版本需要引入react 变量
 * 但是新的版本不需要引入 
 * import React from 'react';
 */
ReactDOM.render(
  <h1>hello</h1>,document.getElementById('root')
)